# Summarise parameter error for simulated HMMs

Summarise parameter error for simulated HMMs

## Usage

``` r
summarise_hmm_parameter_error(estimated_parameters, true_parameters)
```

## Arguments

- estimated_parameters:

  Estimated parameter list.

- true_parameters:

  True parameter list.

## Value

A one-row data frame of error summaries.
